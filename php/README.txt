Here's some simple PHP code that will take a posted
value (from an HTTP post), and then double the
value as a result, sending things back as JSON.

For use with the iOS and Android HTTP example code.
